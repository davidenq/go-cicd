package infra

import "testing"

func TestServerInstance(t *testing.T) {
	t.Run("should return nil when the server is running", func(t *testing.T) {
		/*err := NewServer()

		if err != nil {
			t.Error(err)
		}*/
	})
}
